<UserControl x:Class="PhotoLayout.Views.PhotoPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhotoLayout.Views"
             xmlns:controls="clr-namespace:PhotoLayout.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- New photo name -->
        <TextBlock Text="This is where the name will be (if there is one)" FontSize="22" Foreground="White" />

        <Grid Grid.Row="1">
            <controls:LayoutGrid />
            <TextBlock Text="Layout Grid lives here" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Wheat" FontSize="22" />
        </Grid>
    </Grid>
</UserControl>
